/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { OPPORTUNITY_MARKET_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const FINALIZE_REVEAL_VOUCH_DISCRIMINATOR = new Uint8Array([
  1, 34, 40, 131, 236, 85, 1, 25,
]);

export function getFinalizeRevealVouchDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_REVEAL_VOUCH_DISCRIMINATOR
  );
}

export type FinalizeRevealVouchInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountVouchAccount extends string | AccountMeta<string> = string,
  TAccountOption extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountOwner extends string
        ? ReadonlyAccount<TAccountOwner>
        : TAccountOwner,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountVouchAccount extends string
        ? WritableAccount<TAccountVouchAccount>
        : TAccountVouchAccount,
      TAccountOption extends string
        ? WritableAccount<TAccountOption>
        : TAccountOption,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeRevealVouchInstructionData = {
  discriminator: ReadonlyUint8Array;
  optionId: bigint;
  vouchAccountId: number;
};

export type FinalizeRevealVouchInstructionDataArgs = {
  optionId: number | bigint;
  vouchAccountId: number;
};

export function getFinalizeRevealVouchInstructionDataEncoder(): FixedSizeEncoder<FinalizeRevealVouchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['optionId', getU64Encoder()],
      ['vouchAccountId', getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: FINALIZE_REVEAL_VOUCH_DISCRIMINATOR,
    })
  );
}

export function getFinalizeRevealVouchInstructionDataDecoder(): FixedSizeDecoder<FinalizeRevealVouchInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['optionId', getU64Decoder()],
    ['vouchAccountId', getU32Decoder()],
  ]);
}

export function getFinalizeRevealVouchInstructionDataCodec(): FixedSizeCodec<
  FinalizeRevealVouchInstructionDataArgs,
  FinalizeRevealVouchInstructionData
> {
  return combineCodec(
    getFinalizeRevealVouchInstructionDataEncoder(),
    getFinalizeRevealVouchInstructionDataDecoder()
  );
}

export type FinalizeRevealVouchAsyncInput<
  TAccountSigner extends string = string,
  TAccountOwner extends string = string,
  TAccountMarket extends string = string,
  TAccountVouchAccount extends string = string,
  TAccountOption extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  owner: Address<TAccountOwner>;
  market: Address<TAccountMarket>;
  vouchAccount?: Address<TAccountVouchAccount>;
  option?: Address<TAccountOption>;
  systemProgram?: Address<TAccountSystemProgram>;
  optionId: FinalizeRevealVouchInstructionDataArgs['optionId'];
  vouchAccountId: FinalizeRevealVouchInstructionDataArgs['vouchAccountId'];
};

export async function getFinalizeRevealVouchInstructionAsync<
  TAccountSigner extends string,
  TAccountOwner extends string,
  TAccountMarket extends string,
  TAccountVouchAccount extends string,
  TAccountOption extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: FinalizeRevealVouchAsyncInput<
    TAccountSigner,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountOption,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  FinalizeRevealVouchInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountOption,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    vouchAccount: { value: input.vouchAccount ?? null, isWritable: true },
    option: { value: input.option ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vouchAccount.value) {
    accounts.vouchAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            118, 111, 117, 99, 104, 95, 97, 99, 99, 111, 117, 110, 116,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
        getU32Encoder().encode(expectSome(args.vouchAccountId)),
      ],
    });
  }
  if (!accounts.option.value) {
    accounts.option.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([111, 112, 116, 105, 111, 110])
        ),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
        getU64Encoder().encode(expectSome(args.optionId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.vouchAccount),
      getAccountMeta(accounts.option),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFinalizeRevealVouchInstructionDataEncoder().encode(
      args as FinalizeRevealVouchInstructionDataArgs
    ),
    programAddress,
  } as FinalizeRevealVouchInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountOption,
    TAccountSystemProgram
  >);
}

export type FinalizeRevealVouchInput<
  TAccountSigner extends string = string,
  TAccountOwner extends string = string,
  TAccountMarket extends string = string,
  TAccountVouchAccount extends string = string,
  TAccountOption extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  owner: Address<TAccountOwner>;
  market: Address<TAccountMarket>;
  vouchAccount: Address<TAccountVouchAccount>;
  option: Address<TAccountOption>;
  systemProgram?: Address<TAccountSystemProgram>;
  optionId: FinalizeRevealVouchInstructionDataArgs['optionId'];
  vouchAccountId: FinalizeRevealVouchInstructionDataArgs['vouchAccountId'];
};

export function getFinalizeRevealVouchInstruction<
  TAccountSigner extends string,
  TAccountOwner extends string,
  TAccountMarket extends string,
  TAccountVouchAccount extends string,
  TAccountOption extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: FinalizeRevealVouchInput<
    TAccountSigner,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountOption,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): FinalizeRevealVouchInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountOwner,
  TAccountMarket,
  TAccountVouchAccount,
  TAccountOption,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    vouchAccount: { value: input.vouchAccount ?? null, isWritable: true },
    option: { value: input.option ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.vouchAccount),
      getAccountMeta(accounts.option),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFinalizeRevealVouchInstructionDataEncoder().encode(
      args as FinalizeRevealVouchInstructionDataArgs
    ),
    programAddress,
  } as FinalizeRevealVouchInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountOption,
    TAccountSystemProgram
  >);
}

export type ParsedFinalizeRevealVouchInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    owner: TAccountMetas[1];
    market: TAccountMetas[2];
    vouchAccount: TAccountMetas[3];
    option: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: FinalizeRevealVouchInstructionData;
};

export function parseFinalizeRevealVouchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedFinalizeRevealVouchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      owner: getNextAccount(),
      market: getNextAccount(),
      vouchAccount: getNextAccount(),
      option: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFinalizeRevealVouchInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
