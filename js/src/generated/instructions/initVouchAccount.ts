/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { OPPORTUNITY_MARKET_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const INIT_VOUCH_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  170, 190, 149, 246, 116, 216, 171, 230,
]);

export function getInitVouchAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_VOUCH_ACCOUNT_DISCRIMINATOR
  );
}

export type InitVouchAccountInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountVouchAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountOwner extends string
        ? ReadonlyAccount<TAccountOwner>
        : TAccountOwner,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      TAccountVouchAccount extends string
        ? WritableAccount<TAccountVouchAccount>
        : TAccountVouchAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitVouchAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  vouchAccountId: number;
};

export type InitVouchAccountInstructionDataArgs = { vouchAccountId: number };

export function getInitVouchAccountInstructionDataEncoder(): FixedSizeEncoder<InitVouchAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['vouchAccountId', getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_VOUCH_ACCOUNT_DISCRIMINATOR })
  );
}

export function getInitVouchAccountInstructionDataDecoder(): FixedSizeDecoder<InitVouchAccountInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['vouchAccountId', getU32Decoder()],
  ]);
}

export function getInitVouchAccountInstructionDataCodec(): FixedSizeCodec<
  InitVouchAccountInstructionDataArgs,
  InitVouchAccountInstructionData
> {
  return combineCodec(
    getInitVouchAccountInstructionDataEncoder(),
    getInitVouchAccountInstructionDataDecoder()
  );
}

export type InitVouchAccountAsyncInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountMarket extends string = string,
  TAccountVouchAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** No signature required: this instruction is permissionless. */
  owner: Address<TAccountOwner>;
  market: Address<TAccountMarket>;
  vouchAccount?: Address<TAccountVouchAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  vouchAccountId: InitVouchAccountInstructionDataArgs['vouchAccountId'];
};

export async function getInitVouchAccountInstructionAsync<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountMarket extends string,
  TAccountVouchAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: InitVouchAccountAsyncInput<
    TAccountPayer,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitVouchAccountInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: false },
    vouchAccount: { value: input.vouchAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vouchAccount.value) {
    accounts.vouchAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            118, 111, 117, 99, 104, 95, 97, 99, 99, 111, 117, 110, 116,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
        getU32Encoder().encode(expectSome(args.vouchAccountId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.vouchAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitVouchAccountInstructionDataEncoder().encode(
      args as InitVouchAccountInstructionDataArgs
    ),
    programAddress,
  } as InitVouchAccountInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountSystemProgram
  >);
}

export type InitVouchAccountInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountMarket extends string = string,
  TAccountVouchAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** No signature required: this instruction is permissionless. */
  owner: Address<TAccountOwner>;
  market: Address<TAccountMarket>;
  vouchAccount: Address<TAccountVouchAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  vouchAccountId: InitVouchAccountInstructionDataArgs['vouchAccountId'];
};

export function getInitVouchAccountInstruction<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountMarket extends string,
  TAccountVouchAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: InitVouchAccountInput<
    TAccountPayer,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitVouchAccountInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountOwner,
  TAccountMarket,
  TAccountVouchAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: false },
    vouchAccount: { value: input.vouchAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.vouchAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitVouchAccountInstructionDataEncoder().encode(
      args as InitVouchAccountInstructionDataArgs
    ),
    programAddress,
  } as InitVouchAccountInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMarket,
    TAccountVouchAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitVouchAccountInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /** No signature required: this instruction is permissionless. */
    owner: TAccountMetas[1];
    market: TAccountMetas[2];
    vouchAccount: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitVouchAccountInstructionData;
};

export function parseInitVouchAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitVouchAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      owner: getNextAccount(),
      market: getNextAccount(),
      vouchAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitVouchAccountInstructionDataDecoder().decode(instruction.data),
  };
}
